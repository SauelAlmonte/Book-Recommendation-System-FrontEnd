# =============================================================================
# Template only — safe to commit. Copy to `.env.local` and adjust (or keep in sync).
#
# `.env.local` is gitignored; never put real secrets in NEXT_PUBLIC_* (client-visible).
# Restart `npm run dev` after env changes.
# =============================================================================

# API origin only — no path. See docs/BOOK_API_INTEGRATION.md for contract and examples.
NEXT_PUBLIC_BOOK_API_URL=https://your-api-host.example.com
